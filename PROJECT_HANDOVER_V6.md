# 连山壮瑶同城 (LsLife) - V6.0 商业版项目交接文档

## 📌 项目当前状态与客观现实
本项目是一款针对县域级市场量身打造的**本地同城生活服务交易与通讯平台**。
目前项目已经顺利跨越 V5.0，正式迈入 **V6.0 (商业版)** 阶段。近期我们彻底修复了客户端 UI 的各类痛点，统一了高级视觉审美，重构了二级类目的核心排版，系统已经高度稳定，打通了编译发布链路，随时可用于生产环境的规模化推广。

**服务器 Root 权限已解锁：**
- **IP地址**: `115.191.6.95`
- **系统**: `Ubuntu 24.04`
- **超级管理员账号**: `root`
- **超级管理员密码**: `Maxence2468;`
- **数据库 (PostgreSQL) 宿主机端口**: `5433` (容器内 `5432`)
- **数据库密码**: `af4a98b163543c58c46bf827bdd546a8`

---

## 🏗 最新功能演进与核心修正 (V6.0)

### 1. 顶级商业化视觉与图标矩阵 (Joybuy Design System)
*   全面弃用了旧版的灰暗图标与纸箱占位图。
*   通过编写底层的 `pg` 直连脚本与 SSH 隧道脚本 (`ssh_restore_icons.mjs`)，直接绕过 Prisma 本地驱动限制，**在生产库中强行重塑了 15 个（个人闲置 x8，同城生鲜 x7）高级 3D 扁平化图标的映射**。
*   全站涵盖数码 3C、服饰箱包、海鲜水产、肉禽蛋品等，统一采用了 8k 渲染级别的 3D 黏土风 (Claymorphism) 材质，视觉体验达到一线大厂水准。

### 2. 二级分类页瀑布流重构 (Staggered Grid)
*   针对“房屋租售”、“个人闲置”等二级分类商品/服务列表页 (`CategoryDetailScreen.kt`)，废弃了原有的 `LazyColumn` 单列布局。
*   彻底重构为 **双列非对称瀑布流排列 (`LazyVerticalStaggeredGrid`)**。
*   完美兼容且对齐了首页的 UI 逻辑，显著提升了同屏商品的曝光率与视觉呼吸感。

### 3. 一键打包与归档流水线
*   完善了 `build_apk.ps1` 脚本，执行后自动读取 `jbr` (JDK 17) 环境变量。
*   执行 Gradle `assembleRelease`，并将生成的 V6.0 APK 规范化输出至 `D:\LsLife\releases\` 目录。

---

## 💻 技术栈底座

### Android 客户端 (Frontend)
*   **语言 / SDK**: Kotlin / Min SDK 24 / Target SDK 34
*   **UI 框架**: Jetpack Compose / Material3 / `LazyVerticalStaggeredGrid`
*   **架构管理**: MVVM 单向数据流 / Dagger Hilt 依赖注入

### 服务端与数据存储 (Backend)
*   **环境**: Node.js / TypeScript / PM2 热载托管 (`115.191.6.95`)
*   **数据库**: PostgreSQL 16 (基于 Docker 部署)
*   **ORM / 脚本注入**: Prisma (常规查询) + `pg` & `node-ssh` (用于底层强制刷新)

---

## 🚀 自动化发版与操作指引

1. **直接生产库更新**: 如果再次遭遇 Prisma Sqlite 限制，请直接使用 `d:\LsLife\backend\scripts\ssh_restore_icons.mjs`。
2. **打包 APK**: 在 `D:\LsLife\` 根目录下执行 `.\build_apk.ps1` 即可生成 Release 包。

---

## 🎯 二次开发交接与下一阶段指引

接手此项目进行深度二次开发的新会话，请优先考虑以下高价值方向：

1. **数据容灾与备份机配置**：配置服务器数据库及图片资源的定时异地容灾备份脚本。
2. **高级全局搜索与多维过滤**：实现类似“价格区间” + “成色” 的多维联合聚合搜索。
3. **地图与 O2O 闭环定位卡片**：接入地图 API，在 IM 聊天中实现 `type="location"` 卡片互发。
4. **性能监控与上报**：考虑到目前瀑布流加载大量 3D 高清图标，可接入图片预加载优化及内存监控体系。
